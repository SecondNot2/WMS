NEXT_PUBLIC_API_URL="http://localhost:4000"

# Supabase Storage — dùng cho upload ảnh sản phẩm/avatar
# Tạo bucket public tên `wms-uploads` trong Supabase Dashboard → Storage
# Policy: cho phép `anon` insert/select trên bucket này
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
